
function createLog(sheetName= 'Scheduler') {
  Cord4_Script.createLog(getSheetId(), sheetName);
}

function resetSchedulerLogs(sheetName= 'Scheduler') {
  Cord4_Script.resetSchedulerLogs(getSheetId(), sheetName);
}